import { AnimationIntoDirective } from './animation-into.directive';

describe('AnimationIntoDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationIntoDirective();
    expect(directive).toBeTruthy();
  });
});
