import { SlipHoverDirective } from './slip-hover.directive';

describe('SlipHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new SlipHoverDirective();
    expect(directive).toBeTruthy();
  });
});
